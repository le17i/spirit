{{!< nobrandtemplate}}

<main class="page-container loop author" role="main" itemtype="http://schema.org/Person">

  {{#author}}
  <header="page-container__header center">

    {{#if image}}
			<figure class="author-avatar">
				<img src="{{image}}" alt="{{name}}" itemprop="image" />
			</figure>
		{{/if}}

    <div class="author-bio">
      <h1 class="page-container__header--title" itemprop="name">{{name}}</h1>

      {{#if bio}}
        <p class="author-bio__about">
          {{{bio}}}
        </p>
      {{/if}}
    </div>
  </header>
  {{/author}}

  {{> "loop"}}
</main>
